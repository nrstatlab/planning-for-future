# Site generation.
markdown>=3.5

# Lab verification for Courses 8-10. The notes, Courses 1-5 labs and the
# syllabus extractor need nothing beyond the Python standard library.
numpy>=1.26
# pandas 3, NOT 2. Courses 8, 9 and 12 A assert pandas 3 behaviour --
# groupby().apply() dropping the grouping column, and chained assignment
# failing silently rather than warning. On pandas 2 those labs fail.
pandas>=3.0
scikit-learn>=1.4
mlxtend>=0.23
mongomock>=4.1
openpyxl>=3.1        # Course 9 practical 9: read_excel / to_excel

# Course 9 Unit 5 names three plotting libraries. matplotlib is required;
# seaborn and plotly are imported conditionally by practical 18, which skips
# its section (without failing) if they are absent.
matplotlib>=3.8
seaborn>=0.13
plotly>=5.20

# --- Semester V ---
# Course 12 B (Big Data): real Avro, real Parquet, and Hive-style SQL.
pyarrow>=15.0
fastavro>=1.9
duckdb>=1.0

# Course 13 A (AI): a small Prolog engine. Its limits are real -- no list
# terms, no arithmetic evaluation, no cut, no DCG, arity >= 1 -- and
# tools/run_ai_labs.py PROVES each limit before working around it.
pytholog>=2.4

# PySpark is deliberately NOT here. It needs a JRE and its own build
# environment, and a failed install must not break the other suites, so it
# lives in its own virtualenv: run tools/setup_spark.sh.

# --- Semester VI ---
# Course 14 A (Deep Learning) and 15 A (NLP): PyTorch on CPU, plus Keras 3
# driven by the torch backend (KERAS_BACKEND=torch), which is how the
# syllabus's "Keras/TensorFlow" content is executed here without TensorFlow.
torch>=2.2
keras>=3.0

# Course 14 B (Time Series): ARIMA, SARIMA, VAR, state-space and spectral
# analysis are all real statsmodels calls.
statsmodels>=0.14

# Course 15 A (NLP): both run for real. The NLTK corpora are fetched by
# tools/fetch_nlp_data.py, and the spaCy model is a pip package.
nltk>=3.8
spacy>=3.7
# python3 -m spacy download en_core_web_sm

# Course 15 B (MLOps): real experiment tracking, real data versioning, and a
# real model endpoint. MLflow 3 requires a database backend -- the old
# ./mlruns file store is in maintenance mode and raises -- so the lab uses
# sqlite:///mlflow.db, which is what MLflow itself recommends.
# NOTE: installing mlflow or dvc can pull pandas back to 2.x, which BREAKS
# Courses 8, 9 and 12 A -- they are written against pandas 3. If that
# happens, reinstall it: pip install --force-reinstall --no-deps pandas==3.0.5
mlflow>=3.0
dvc>=3.0
flask>=3.0
scipy>=1.11

# transformers installs, but NO PRETRAINED MODEL CAN BE FETCHED here:
# huggingface.co is refused by the egress policy with a 403 at the gateway.
# Every Hugging Face experiment is therefore marked NOT EXECUTED, and the
# runnable half builds the mechanism -- self-attention, an encoder block --
# from scratch in PyTorch instead.
# transformers>=4.38
